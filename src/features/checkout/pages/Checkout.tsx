import { useForm } from 'react-hook-form';

const Checkout = () => {
  const { register } = useForm();
  return (
    <section>
      <h1>Checkout</h1>
    </section>
  );
};

export default Checkout;
